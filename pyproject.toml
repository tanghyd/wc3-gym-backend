[project]
name = "app"
version = "1.0.0"
description = "FastAPI backend for the GNL (Gym Newbie League) esports platform"
requires-python = ">=3.13"
dependencies = [
    "requests",
    "gspread",
    "pandas",
    "openpyxl",
    "python-dotenv",
    "apscheduler",
    "pyodbc",
    "pymysql",
    "sqlalchemy>=2,<3",
    "pydantic>=2,<3",
    "pyjwt",
    "fastapi>=0.141.1",
    "uvicorn>=0.52.3",
    "python-multipart>=0.0.32",
    "sqlmodel>=0.0.39",
    "alembic>=1.19.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "httpx2>=2.10.0",
    "pytest",
    "ruff>=0.16.3",
    "rust-just>=1.58.0",
]

[tool.ruff.lint]
extend-select = ["ANN"]  # flake8-annotations

# Raised lint errors in legacy code that were suppressed rather than addressed.
ignore = [
    "BLE001",  # blind `except Exception`
    "DTZ001",  # naive datetimes match the DATETIME columns
    "PLW0211", # staticmethod argument named cls
    "RUF012",  # mutable class attribute default
    "S110",    # try-except-pass
    "TRY002",  # raise Exception
    "TRY004",  # fixing it changes the raised exception type
    "TRY401",  # redundant exception object in logging.exception
]

[tool.ruff.lint.per-file-ignores]
"app/models/types.py" = ["ANN401"]  # any type

[tool.pytest]
testpaths = ["tests"]
